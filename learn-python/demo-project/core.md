# Core Python Concepts

- Organize a small runnable project around a clear entry point and supporting modules.
- Keep dependencies explicit with `requirements.txt` and isolated environments.
- Separate configuration, application logic, and external integrations.
- Write code that can be run locally, tested, and expanded without large rewrites.
- Treat a demo project as a place to practice packaging, imports, and execution flow.
