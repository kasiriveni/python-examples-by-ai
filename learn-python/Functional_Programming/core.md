# Core Python Concepts

- Prefer small pure functions when predictable transformations matter.
- Use higher-order functions that accept or return other functions.
- Apply `map`, `filter`, and `reduce` when they improve clarity over manual loops.
- Use lambda expressions sparingly for short inline behavior.
- Understand closures and lexical scope for function factories and decorators.
- Combine functions into pipelines without introducing unnecessary shared state.
