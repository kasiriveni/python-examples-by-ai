# Core Python Concepts

- Learn Python syntax, indentation, comments, and how scripts execute line by line.
- Use variables to store numbers, strings, and boolean values.
- Practice operators for arithmetic, comparison, and simple logic.
- Write `if`, `elif`, and `else` blocks to make decisions.
- Use `for` and `while` loops to repeat work.
- Define simple functions to organize code and reuse logic.
