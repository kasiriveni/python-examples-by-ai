# Core Python Concepts

## Core Themes
- Python syntax, indentation, and language rules.
- Introductory control flow and function definitions.
- Comments, identifiers, input, and simple variable handling.

## Core Theme Examples
- Example 1: Proper indentation and language syntax rules.
- Example 2: Function definition with if-else branching.
- Example 3: Variable assignment with meaningful identifiers.

## Files and Concepts
- comments.py: single-line comments, multi-line comments
- control_flow_basics.py: if and elif branches, ternary expressions, loops, enumerate, zip
- functions_basics.py: function definitions, default arguments, args, kwargs, multiple returns
- introduction.py: basic print statements, simple script output
- keywords_and_identifiers.py: Python keywords, identifier naming rules
- overview_basics.py: variables, user input, type casting
- python_syntax.py: indentation rules, basic if and else syntax

## Core Example
This example shows variables, a function, and a simple conditional.

```python
language = "Python"

def greet(name):
	return f"Hello, {name}!"

message = greet(language)
if language.startswith("Py"):
	print(message)
```
