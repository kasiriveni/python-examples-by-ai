# Core Python Concepts

- Build fluency with variables, strings, numbers, and built-in collection types.
- Use conditionals and loops to express basic program logic.
- Write functions with parameters, return values, and clear names.
- Read from and write to files using `with` blocks.
- Catch and handle simple exceptions without hiding useful errors.
- Split code into small modules as programs grow.
