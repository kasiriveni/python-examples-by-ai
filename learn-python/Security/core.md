# Core Python Concepts

- Validate input and treat all external data as potentially unsafe.
- Store passwords and secrets securely instead of embedding them in code.
- Use vetted cryptography and hashing libraries rather than homemade security code.
- Apply authentication and authorization checks deliberately.
- Keep dependencies updated and review them for known risk.
- Design Python services so security is built into normal control flow, not added later.
