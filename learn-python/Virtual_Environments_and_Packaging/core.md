# Core Python Concepts

- Create isolated environments so each project uses the dependencies it actually needs.
- Install packages into the active environment and record them for reproducibility.
- Understand how `venv`, `pip`, and packaging metadata work together.
- Use `pyproject.toml`, requirements files, or lock files to define environment state.
- Connect local development, testing, and distribution through consistent packaging practices.
