# Core Python Concepts

- Define package metadata and build settings in `pyproject.toml`.
- Understand source distributions, wheels, and how Python packages are built.
- Use semantic versioning so changes communicate compatibility clearly.
- Publish packages with complete metadata, dependency constraints, and entry points.
- Treat packaging as part of API design because other projects will depend on it.
