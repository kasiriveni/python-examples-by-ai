# Example: Python Syntax and Indentation
# Demonstrates Python's indentation rules

def greet(name):
    if name:
        print(f"Hello, {name}!")
    else:
        print("Hello, World!")

greet("AI Engineer")
greet("")

# REPL Usage
# Open a terminal and type `python` to start the REPL.
# Example commands:
# >>> x = 10
# >>> print(x)
# >>> def add(a, b): return a + b
# >>> add(5, 3)
