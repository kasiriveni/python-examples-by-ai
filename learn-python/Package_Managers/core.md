# Core Python Concepts

- Use package managers to install, upgrade, and remove project dependencies.
- Understand when to use `pip`, Conda, or newer tools such as `uv`.
- Keep dependency files current so environments can be recreated reliably.
- Install packages into the correct virtual environment instead of the global interpreter.
- Evaluate tools based on workflow, lockfile support, and team reproducibility needs.
