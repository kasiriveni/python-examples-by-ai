# Core Python Concepts

- Split large programs into modules so code is easier to read and reuse.
- Group related modules into packages with clear public interfaces.
- Use absolute and relative imports intentionally and avoid circular dependencies.
- Understand how `__init__.py` and module execution affect package behavior.
- Keep reusable logic separate from scripts and entry-point code.
- Design package structure around maintainability rather than folder count.
