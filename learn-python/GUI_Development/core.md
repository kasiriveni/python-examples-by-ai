# Core Python Concepts

- Build interfaces from widgets, containers, and layout managers.
- Use event-driven programming so callbacks respond to user actions.
- Keep UI code separate from business logic and data transformation.
- Manage dialogs, menus, forms, and validation in a predictable flow.
- Be careful with threading so long-running work does not freeze the interface.
- Use drawing surfaces or custom widgets when standard controls are not enough.
