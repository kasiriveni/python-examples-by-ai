# Core Python Concepts

- Call LLM APIs safely with clear request and response handling.
- Build prompts deliberately and separate prompt construction from transport code.
- Add retries, timeouts, and validation around external AI service calls.
- Handle structured outputs, tool calls, and streaming responses when supported.
- Protect API keys and sensitive data throughout the request lifecycle.
- Measure token usage, latency, and error rates as part of normal integration work.
